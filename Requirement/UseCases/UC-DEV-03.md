# UC-DEV-02: Liên kết các Đối tượng (dùng @ID)

<table><tbody><tr><td>ID and Name:</td><td>UC-DEV-02: Liên kết các Đối tượng (dùng @ID)</td><td>&nbsp;</td><td>&nbsp;</td></tr><tr><td>Created By:</td><td>&nbsp;</td><td>Date Created:</td><td>&nbsp;</td></tr><tr><td>Primary Actor:</td><td>Business Analyst (BA)</td><td>Secondary Actors:</td><td>&nbsp;</td></tr><tr><td>Description:</td><td colspan="3">Use case này mô tả cách BA tạo ra các mối quan hệ (traceability) giữa các Đối tượng Yêu cầu (Artifacts) bằng cách sử dụng cú pháp tag <code>@ID</code>.</td></tr><tr><td>Trigger:</td><td colspan="3">BA đang chỉnh sửa một đối tượng trong "Form View" (theo <code>UC-DEV-01</code>).</td></tr><tr><td>Preconditions:</td><td colspan="3">1. BA đang ở trong "Form View" (<code>UC-DEV-01</code>).<br>2. Hệ thống đã có ít nhất một đối tượng khác đã được tạo (ví dụ: <code>@BR001</code>).<br>3. Form của đối tượng hiện tại có một trường (field) hỗ trợ liên kết (ví dụ: <code>TextArea</code>, <code>Linker</code>, hoặc <code>Flow Builder</code>).</td></tr><tr><td>Postconditions:</td><td colspan="3"><b>Thành công:</b> Một liên kết 2 chiều được thiết lập. File <code>.json</code> của đối tượng (ví dụ: <code>UC001.json</code>) lưu trữ liên kết (ví dụ: <code>links: ["@BR001"]</code>). Hệ thống cập nhật chỉ mục "Backlinks" cho <code>@BR001</code> (<code>UC-MOD-03</code>).<br><b>Thất bại:</b> Liên kết không được tạo.</td></tr><tr><td>Normal Flow:</td><td colspan="3">1.0. BA đang chỉnh sửa <code>UC001</code> (trong "Form View").<br>2.0. Trong một trường <code>TextArea</code> (ví dụ: "Mô tả") hoặc trường <code>Linker</code>, BA gõ phím <code>@</code>.<br>3.0. Hệ thống ngay lập tức kích hoạt một cửa sổ (popup/dropdown) tìm kiếm tự động hoàn thành (autocomplete).<br>4.0. Cửa sổ tìm kiếm hiển thị danh sách tất cả các đối tượng trong dự án (ví dụ: <code>@BR001: Rule Đăng nhập</code>, <code>@SCR005: Màn hình Login</code>).<br>5.0. BA tiếp tục gõ (ví dụ: <code>@BR</code>) để lọc danh sách.<br>6.0. BA chọn <code>@BR001</code> từ danh sách (bằng chuột hoặc phím Enter).<br>7.0. Hệ thống chèn tag <code>@BR001</code> (dưới dạng một đối tượng đồ họa/hyperlink) vào trường.<br>8.0. (Auto-save) Hệ thống lưu lại file <code>UC001.json</code> với mảng liên kết được cập nhật.<br>9.0. Hệ thống (ở chế độ nền) cập nhật chỉ mục "Backlinks" cho <code>@BR001</code> để biết rằng <code>UC001</code> đang liên kết đến nó.</td></tr><tr><td>Alternative Flows:</td><td colspan="3"><b>1.0.A1: Xem nhanh thông tin (Hover)</b><br>Tại bất kỳ thời điểm nào, BA di chuột (hover) qua một tag <code>@BR001</code> đã tồn tại.<br>1.1. Hệ thống hiển thị một cửa sổ (tooltip/popup) nhỏ tóm tắt thông tin của <code>@BR001</code> (Tên, Mô tả, Trạng thái).<br><br><b>1.0.A2: Điều hướng (Click)</b><br>Tại bất kỳ thời điểm nào, BA nhấn <code>Ctrl + Click</code> (hoặc click) vào tag <code>@BR001</code>.<br>1.1. Hệ thống mở một Tab mới cho đối tượng <code>@BR001</code>.</td></tr><tr><td>Exceptions:</td><td colspan="3"><b>1.0.E1: Liên kết đến ID không tồn tại</b><br>Tại bước 7.0:<br>7.1. BA gõ tay một ID không có thật (ví dụ: <code>@BR999</code>) và không chọn từ danh sách.<br>7.2. Hệ thống hiển thị tag <code>@BR999</code> dưới dạng văn bản bình thường (hoặc gạch chân màu đỏ) để báo hiệu liên kết bị hỏng (broken link).<br>7.3. Liên kết không được lưu vào mảng liên kết trong file <code>.json</code>.</td></tr><tr><td>Priority:</td><td colspan="3">Cao</td></tr><tr><td>Frequency of Use:</td><td colspan="3">Rất Cao (Hàng ngày)</td></tr><tr><td>Business Rules:</td><td colspan="3"><code>BR-LINK-01</code>: Tất cả các liên kết phải được lưu trữ dưới dạng <code>@ID</code> (không phải tên) để đảm bảo tính toàn vẹn ngay cả khi tên đối tượng thay đổi.</td></tr><tr><td>Other Information:</td><td colspan="3">Đây là tính năng cốt lõi để xây dựng Ma trận Truy vết (RTM) và Sơ đồ Quan hệ (Graph View - <code>UC-MOD-02</code>).</td></tr><tr><td>Assumptions:</td><td colspan="3">N/A</td></tr></tbody></table>

## Guideline

Định danh cho luồng bình thường của use case này là 1.0. Các luồng thay thế được xác định bằng cách tăng số ở bên phải dấu chấm, vì vậy luồng thay thế đầu tiên là 1.1, luồng thứ hai sẽ là 1.2, v.v. Cả luồng bình thường và luồng thay thế đều có thể có các ngoại lệ riêng. Ngoại lệ đầu tiên trên luồng bình thường của use case số 4 sẽ được gắn nhãn 1.0.E1. Ngoại lệ thứ hai cho luồng thay thế đầu tiên của use case này sẽ là 1.1.E2.