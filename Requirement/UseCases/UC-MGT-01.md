# UC-MGT-01: Quản lý Tác vụ (Todo List)

<table><tbody><tr><td>ID and Name:</td><td>UC-MGT-01: Quản lý Tác vụ (Todo List)</td><td>&nbsp;</td><td>&nbsp;</td></tr><tr><td>Created By:</td><td>&nbsp;</td><td>Date Created:</td><td>&nbsp;</td></tr><tr><td>Primary Actor:</td><td>Business Analyst (BA)</td><td>Secondary Actors:</td><td>&nbsp;</td></tr><tr><td>Description:</td><td colspan="3">Use case này cho phép BA tạo và theo dõi các công việc (tasks) cần thiết để hoàn thành các Đối tượng Yêu cầu. Đây là một dạng "Todo List" cá nhân hoặc của nhóm, giúp lập kế hoạch hoàn thiện tài liệu (ví dụ: "Draft UC-005", "Review BR-010").</td></tr><tr><td>Trigger:</td><td colspan="3">BA chọn "Tạo mới" > "Task", hoặc từ một Dashboard Kế hoạch.</td></tr><tr><td>Preconditions:</td><td colspan="3">1. BA đã mở một dự án (tham chiếu <code>UC-PM-02</code>).<br>2. BA đã định nghĩa "Loại Đối tượng" (Artifact Type) cho "Task" (ví dụ: <code>@TSK</code>) trong <code>UC-CFG-01</code>.</td></tr><tr><td>Postconditions:</td><td colspan="3"><b>Thành công:</b> Một đối tượng Task mới (ví dụ: <code>@TSK001</code>) được tạo và lưu lại. Task này có thể được liên kết với các yêu cầu khác.<br><b>Thất bại:</b> Task không được tạo.</td></tr><tr><td>Normal Flow:</td><td colspan="3">1.0. BA kích hoạt (Trigger) use case (ví dụ: nhấn nút "Tạo Task").<br>2.0. Hệ thống mở "Form View" cho một đối tượng "Task" mới (dựa trên template đã định nghĩa trong <code>UC-CFG-01</code>).<br>3.0. BA điền thông tin vào các trường (fields) của Form:<br>    - "Tên công việc" (ví dụ: "Hoàn thiện draft UC-001")<br>    - "Trạng thái" (Dropdown: <code>Todo</code>, <code>In Progress</code>, <code>Done</code>). Mặc định là <code>Todo</code>.<br>    - "Người thực hiện" (Tùy chọn).<br>    - "Ngày hết hạn (Due Date)" (Tùy chọn).<br>4.0. BA sử dụng trường <code>Linker</code> (tham chiếu <code>UC-DEV-02</code>) để liên kết Task này với yêu cầu mà nó phục vụ (ví dụ: <code>@UC001</code>).<br>5.0. (Auto-save) Hệ thống lưu lại đối tượng Task mới (ví dụ: <code>TSK001.json</code> và <code>TSK001.md</code>).<br>6.0. Đối tượng Task này ngay lập tức xuất hiện trong các Dashboard Kế hoạch (tham chiếu <code>UC-MGT-02</code>).</td></tr><tr><td>Alternative Flows:</td><td colspan="3"><b>1.0.A1: Tạo Task nhanh từ một đối tượng</b><br>Tại bước 1.0:<br>1.1. BA đang xem <code>UC001</code>.<br>1.2. BA nhấp chuột phải (hoặc vào menu "...") và chọn "Tạo Task liên quan".<br>1.3. Hệ thống thực hiện bước 2.0 (mở Form Task).<br>1.4. Hệ thống tự động điền trường <code>Linker</code> (bước 4.0) với <code>@UC001</code>.<br>1.5. Use case tiếp tục từ bước 3.0.</td></tr><tr><td>Exceptions:</td><td colspan="3"><b>1.0.E1: Lỗi lưu (Save Error)</b><br>Tại bước 5.0:<br>5.1. Hệ thống không thể lưu file (lỗi quyền, hết dung lượng).<br>5.2. Hệ thống hiển thị thông báo lỗi.<br>5.3. Use case kết thúc thất bại.</td></tr><tr><td>Priority:</td><td colspan="3">Trung bình</td></tr><tr><td>Frequency of Use:</td><td colspan="3">Cao (Hàng ngày/Hàng tuần)</td></tr><tr><td>Business Rules:</td><td colspan="3">N/A</td></tr><tr><td>Other Information:</td><td colspan="3">Các đối tượng <code>@TSK</code> này là nguồn dữ liệu đầu vào cho <code>UC-MGT-02</code> (Kanban/Gantt Dashboard).</td></tr><tr><td>Assumptions:</td><td colspan="3">Template cho "Task" đã được định nghĩa, bao gồm ít nhất các trường: "Tên", "Trạng thái", và "Linker".</td></tr></tbody></table>

## Guideline

Định danh cho luồng bình thường của use case này là 1.0. Các luồng thay thế được xác định bằng cách tăng số ở bên phải dấu chấm, vì vậy luồng thay thế đầu tiên là 1.1, luồng thứ hai sẽ là 1.2, v.v. Cả luồng bình thường và luồng thay thế đều có thể có các ngoại lệ riêng. Ngoại lệ đầu tiên trên luồng bình thường của use case số 4 sẽ được gắn nhãn 1.0.E1. Ngoại lệ thứ hai cho luồng thay thế đầu tiên của use case này sẽ là 1.1.E2.