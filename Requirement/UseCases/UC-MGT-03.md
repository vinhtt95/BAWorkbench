# UC-MGT-03: Gán Yêu cầu vào Phiên bản (Release)

<table><tbody><tr><td>ID and Name:</td><td>UC-MGT-03: Gán Yêu cầu vào Phiên bản (Release)</td><td>&nbsp;</td><td>&nbsp;</td></tr><tr><td>Created By:</td><td>&nbsp;</td><td>Date Created:</td><td>&nbsp;</td></tr><tr><td>Primary Actor:</td><td>Business Analyst (BA)</td><td>Secondary Actors:</td><td>&nbsp;</td></tr><tr><td>Description:</td><td colspan="3">Use case này cho phép BA quản lý phạm vi (scope) của sản phẩm bằng cách gán các Đối tượng Yêu cầu đã hoàn thiện (ví dụ: trạng thái "Approved") vào một Phiên bản (Release) cụ thể đã được định nghĩa (ví dụ: "Version 1.0").</td></tr><tr><td>Trigger:</td><td colspan="3">BA đang xem một Đối tượng Yêu cầu (<code>@UC001</code>) và muốn gán nó vào một Release.</td></tr><tr><td>Preconditions:</td><td colspan="3">1. BA đã mở một dự án (tham chiếu <code>UC-PM-02</code>).<br>2. BA đã định nghĩa ít nhất một "Release" (ví dụ: <code>@REL001: V1.0</code>) trong Cài đặt (tham chiếu <code>UC-CFG-02</code>).<br>3. BA đã có ít nhất một yêu cầu (ví dụ: <code>@UC001</code>) ở trạng thái "Approved" (tham chiếu <code>UC-MGT-02</code>).<br>4. Template của <code>@UC001</code> (từ <code>UC-CFG-01</code>) đã có một trường (field) "Target Release" (Loại <code>Linker</code> hoặc <code>Dropdown</code>).</td></tr><tr><td>Postconditions:</td><td colspan="3"><b>Thành công:</b> Đối tượng Yêu cầu (ví dụ: <code>UC001.json</code>) được cập nhật, lưu trữ liên kết đến "Release" (ví dụ: <code>"targetRelease": "@REL001"</code>).<br><b>Thất bại:</b> Không có thay đổi nào được lưu.</td></tr><tr><td>Normal Flow:</td><td colspan="3">1.0. BA mở đối tượng yêu cầu đã "Approved" (ví dụ: <code>@UC001</code>) trong "Form View" (theo <code>UC-DEV-01</code>).<br>2.0. BA tìm đến trường (field) "Target Release" trên Form.<br>3.0. BA nhấn vào <code>Dropdown</code> (hoặc <code>Linker</code>) của trường "Target Release".<br>4.0. Hệ thống hiển thị danh sách các "Release" đã tạo (ví dụ: <code>@REL001: V1.0</code>, <code>@REL002: V2.0</code>).<br>5.0. BA chọn <code>@REL001: V1.0</code>.<br>6.0. (Auto-save) Hệ thống lưu lại liên kết này vào file <code>UC001.json</code>.</td></tr><tr><td>Alternative Flows:</td><td colspan="3"><b>1.0.A1: Gán hàng loạt (Batch Assign) từ Kanban Dashboard</b><br>1.1. BA mở "Dashboard Kế hoạch" (Kanban View) (theo <code>UC-MGT-02</code>).<br>1.2. BA đang ở cột "Approved".<br>1.3. BA chọn nhiều thẻ yêu cầu (<code>@UC001</code>, <code>@UC002</code>, <code>@FN005</code>).<br>1.4. BA nhấn chuột phải > "Assign to Release...".<br>1.5. Hệ thống hiển thị danh sách các "Release" (như bước 4.0).<br>1.6. BA chọn <code>@REL001: V1.0</code>.<br>1.7. Hệ thống (Tự động hóa) lặp (loop) qua các đối tượng đã chọn và cập nhật trường "Target Release" cho tất cả chúng (lưu vào các file <code>.json</code> tương ứng).</td></tr><tr><td>Exceptions:</td><td colspan="3"><b>1.0.E1: Lỗi Auto-save</b><br>Tại bước 6.0:<br>6.1. Hệ thống không thể ghi file (lỗi quyền, file bị khóa).<br>6.2. Hệ thống hiển thị thông báo lỗi (tham chiếu <code>UC-DEV-01</code>, <code>1.0.E1</code>).</td></tr><tr><td>Priority:</td><td colspan="3">Cao</td></tr><tr><td>Frequency of Use:</td><td colspan="3">Trung bình (Thường diễn ra theo đợt, khi chuẩn bị chốt scope cho 1 release)</td></tr><tr><td>Business Rules:</td><td colspan="3">N/A</td></tr><tr><td>Other Information:</td><td colspan="3">Đây là bước bắt buộc để có thể lọc yêu cầu khi xuất bản tài liệu SRS (tham chiếu <code>UC-PUB-01</code>).</td></tr><tr><td>Assumptions:</td><td colspan="3">Trường "Target Release" đã được BA thêm vào các template (<code>UC-CFG-01</code>) mà họ muốn quản lý theo phiên bản.</td></tr></tbody></table>

## Guideline

Định danh cho luồng bình thường của use case này là 1.0. Các luồng thay thế được xác định bằng cách tăng số ở bên phải dấu chấm, vì vậy luồng thay thế đầu tiên là 1.1, luồng thứ hai sẽ là 1.2, v.v. Cả luồng bình thường và luồng thay thế đều có thể có các ngoại lệ riêng. Ngoại lệ đầu tiên trên luồng bình thường của use case số 4 sẽ được gắn nhãn 1.0.E1. Ngoại lệ thứ hai cho luồng thay thế đầu tiên của use case này sẽ là 1.1.E2.